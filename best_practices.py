# 1. simple instructions
    # e.g: 
    # ✅     # name = input("Name: ")       
             #  print(name)

    # ❌     print(input("Name: "))





# 2. one instructions per one task
    #e.g:
    # input  (step 1)
    # calc    (step 2)
    # output   (step 3)




# 3. short and meaningful var names
    # e.g:  use meaningful like ( sum, num1, num2, mult......) names instead of using a, b, c, d ......
    #    --------------- To calculate simple interest ---------------

    # ❌     a = float(input("a: "))
    #        b = float(input("b: "))
    #        c = float(input("c: "))
    #        print(a*b*c/)100

    # ✅     p = float(input("p: "))
    #        r = float(input("r: "))
    #        t = float(input("t: "))
    #        si = (p*r*t/)100
    #        print(si)


# 4. use appropriate  comments




# 5. avoid complex expressions
    # e.g:     Avoid using complex exp like clever if...



